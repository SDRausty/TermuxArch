installdir="/data/data/com.termux/files/home/arch"
spaceinfo () {
#	units="$(df "$installdir" 2>/dev/null | awk 'FNR == 1 {print $2}')" 
declare -a unit="$(df "$installdir" 2>/dev/null)" 
echo $unit
echo $unit[@]
echo $unit[3]
declare -p unit
	units="${unit[@]:3:4}"
declare -p units
	if [[ "$units" = Size ]];then
		spaceinfogsize 
		printf "$spaceMessage"
	elif [[ "$units" = 1K-blocks ]];then
		spaceinfoksize 
		printf "$spaceMessage"
	fi
echo $units
}
spaceinfo 
echo $units
